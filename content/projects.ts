/**
 * ── YOUR PROJECTS ────────────────────────────────────────────────────────────
 * One entry here == one app in the OS. Add an object, get a window, a dock icon
 * and an `open <id>` terminal command for free. No component to write.
 *
 * OWNER: Dummy Projects agent (Phase 1). Replaced with real projects by you.
 */
import type { Project } from './types';

export const projects: Project[] = [];

export default projects;
