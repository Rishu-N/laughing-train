/**
 * ── FACTS ABOUT YOU ──────────────────────────────────────────────────────────
 * These are what players uncover by clearing cells in Fact-sweeper. Each safe
 * cell reveals one, and the Dossier panel fills in as they play.
 *
 * Keep values to one short line. Aim for 40+ so the hardest board can be filled.
 *
 * OWNER: Fact-sweeper agent (Phase 1). Replaced with real facts by you.
 */
import type { Fact } from './types';

export const facts: Fact[] = [];

export default facts;
